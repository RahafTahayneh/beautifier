export enum CodeKeywords {
    const ="const",
    await ="await",
    break = "break",
    case = "case",
    catch = "catch",
    class = "class",
    continue = "continue",
    debugger = "debugger",
    default = "default",
    delete = "delete",
    do = "do",
    else ="else",
    enum = "enum",
    export = "export",
    extends = "extends",
    false = "false",
    finally = "finally",
    for = "for",
    function = "function",
    if ="if",
    implements = "implements",
    import = "import",
    in = "in",
    instanceof = "instanceof",
    interface ="interface",
    let = "let",
    new = "new",
    null = "null",
    package = "package",
    private = "private",
    protected = "protected",
    public = "public",
    return = "return",
    super = "super",
    switch = "switch",
    static = "static",
    this = "this",
    throw = "throw",
    try = "try",
    True = "True",
    typeof = "typeof",
    var = "var",
    void = "void",
    while ="while",
    with = "with",
    yield = "yield"
}

export enum ValueProperties {
    Infinity = "Infinity",
    NaN = "NaN",
    undefined = "undefined",
    globalThis = "globalThis"
}

export enum FunctionProperties {
    eval= "eval",
    isFinite = "isFinite",
    isNaN = "isNaN",
    parseFloat = "parseFloat",
    parseInt = "parseInt",
    encodeURI = "encodeURI",
    encodeURIComponent = "encodeURIComponent",
    decodeURI = "decodeURI",
    decodeURIComponent = "decodeURIComponent"
}

export enum FundamentalObjects {
    Object = "Object",
    Function = "Function",
    Boolean = "Boolean",
    Symbol = "Symbol"
}

export enum ErrorObject {
    Error = "Error",
    AggregateError = "AggregateError",
    EvalError = "EvalError",
    InternalError = "InternalError",
    RangeError = "RangeError",
    ReferenceError = "ReferenceError",
    SyntaxError = "SyntaxError",
    TypeError = "TypeError",
    URIError = "URIError"
}

export enum NumberAndDates {
    Number = "Number",
    BigInt = "BigInt",
    Math = "Math",
    Date = "Date"
}

export enum TextProcessing {
    String= "String",
    RegExp = "RegExp"
}

export enum KeyedCollections {
    Map = "Map",
    Set = "Set",
    WeakMap = "WeakMap",
    WeakSet = "WeakSet"
}

export enum ControlAbstractionObjects {
    Promise = "Promise",
    Generator = "Generator",
    GeneratorFunction = "GeneratorFunction",
    AsyncFunction = "AsyncFunction",
    AsyncGenerator = "AsyncGenerator",
    AsyncGeneratorFunction = "AsyncGeneratorFunction"
}